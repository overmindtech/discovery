name: Tests
on: push
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Go only supports 2x revisions at once
        go-version: [1.16.x, 1.17.x]
    services:
      nats:
        image: nats
        ports:
          - 4222:4222
      nats-auth:
        image: ghcr.io/overmindtech/nats-test-populated:main
        credentials:
          username: dylanratcliffe
          password: ${{ secrets.GHCR_PULL }}
        ports:
          - 4223:4222
      nats-token-exchange:
        image: ghcr.io/overmindtech/nats-token-exchange-test-populated:main
        env:
          NATS_SERVER: nats://nats-auth:4222
        credentials:
          username: dylanratcliffe
          password: ${{ secrets.GHCR_PULL }}
        ports:
          - 8080:8080
    steps:

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: ${{ matrix.go-version }}

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Get dependencies
      run: |
        go get -v -t -d ./...
  
    - name: Test
      env:
        OVERMIND_NTE_ALLPERMS_CLIENT_ID: ${{ secrets.OVERMIND_NTE_ALLPERMS_CLIENT_ID }}
        OVERMIND_NTE_ALLPERMS_CLIENT_SECRET: ${{ secrets.OVERMIND_NTE_ALLPERMS_CLIENT_SECRET }}
        OVERMIND_NTE_ALLPERMS_DOMAIN: ${{ secrets.OVERMIND_NTE_ALLPERMS_DOMAIN }}
      run: go test -timeout 60s ./... -race

  release:
    name: Release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - test
    steps:
      # Creates a release and attaches
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          generateReleaseNotes: true
          token: ${{ secrets.GITHUB_TOKEN }}
